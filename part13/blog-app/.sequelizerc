const path = require("path");

module.exports = {
  config: path.resolve("util", "config.js"),
  "models-path": path.resolve("models"),
  "seeders-path": path.resolve("migrations", "seeders"),
  "migrations-path": path.resolve("migrations"),
};
